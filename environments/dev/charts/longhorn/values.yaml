longhorn:
  # CSI components will use default values from the Longhorn chart
  
  # Service account configuration
  serviceAccount:
    create: true
    name: longhorn-service-account

  # Disable pre-upgrade jobs
  preUpgradeChecker:
    jobEnabled: false
  
  # Default disk configuration for nodes
  defaultSettings:
    # Use local-path storage class for Longhorn PVCs
    defaultDataPath: /var/lib/longhorn/
    createDefaultDiskLabeledNodes: true
    # Force creation of default disk on all nodes
    createDefaultDiskOnLabeledNodes: true
    defaultDataLocality: best-effort
    # Single node cluster settings (for multi-node: set to 3 and enable anti-affinity)
    replicaSoftAntiAffinity: false  # Enable for multi-node clusters
    storageOverProvisioningPercentage: 200
    storageMinimalAvailablePercentage: 10
    # Single node cluster: 1 replica (for multi-node: set to 3)
    defaultReplicaCount: 1
    backupstorePollInterval: 300
    taintToleration: ""
    systemManagedComponentsNodeSelector: "kubernetes.io/os:linux"
    
    priorityClass: ""
    autoSalvage: true
    disableSchedulingOnCordonedNode: true
    replicaZoneSoftAntiAffinity: false  # Enable for multi-zone clusters
    nodeDownPodDeletionPolicy: do-nothing
    allowNodeDrainWithLastHealthyReplica: false
    mkfsExt4Parameters: ""
    disableReplicaRebuild: false
    replicaReplenishmentWaitInterval: 600
    concurrentReplicaRebuildPerNodeLimit: 5
    disableRevisionCounter: false
    systemManagedPodsImagePullPolicy: if-not-present
    allowVolumeCreationWithDegradedAvailability: true
    autoDeletePodWhenVolumeDetachedUnexpectedly: true
    storageNetwork: ""
    kubernetesClusterAutoscalerEnabled: false
    orphanAutoDeletion: false
    storageReservedPercentageForDefaultDisk: 30
    defaultDataLocalityPolicy: best-effort
    fastReplicaRebuildEnabled: false
    replicaDiskSoftAntiAffinity: false  # Enable for multi-node clusters
    allowCollectingLonghornUsageMetrics: true
    backingImageCleanupWaitInterval: 60
    backingImageRecoveryWaitInterval: 300
    guaranteedInstanceManagerCPU: 12
    # Disable upgrade checker to prevent pre-upgrade issues
    upgradeChecker: false

  # Enable persistence for Longhorn manager
  persistence:
    defaultClass: true
    # Single node cluster: 1 replica (for multi-node: set to 3)
    defaultClassReplicaCount: 1
    reclaimPolicy: Delete

  # Enable authentication for Longhorn UI (basic auth)
  # Create secret: kubectl create secret generic basic-auth --from-literal=auth=$(htpasswd -nb admin <password>) -n longhorn-system
  longhornUI:
    replicas: 1  # Single node cluster (for multi-node: set to 2)
  
  # Basic auth configuration
  ingress:
    enabled: true
    ingressClassName: nginx
    host: longhorn.dev.786999.xyz
    tls: true
    tlsSecret: longhorn-tls-lets
    annotations:
      cert-manager.io/cluster-issuer: "letsencrypt-prod"
      external-dns.alpha.kubernetes.io/enabled: "true"
      # Basic auth annotations
      nginx.ingress.kubernetes.io/auth-type: basic
      nginx.ingress.kubernetes.io/auth-secret: basic-auth
      nginx.ingress.kubernetes.io/auth-realm: 'Authentication Required - Longhorn'

  # Resource settings
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

  # Service settings
  service:
    ui:
      type: ClusterIP
