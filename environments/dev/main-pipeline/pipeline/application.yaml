apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: main-pipeline-dev
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/argocd-bootstrap.git
    targetRevision: HEAD
    path: environments/dev/main-pipeline
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
  revisionHistoryLimit: 3
