apiVersion: v1
kind: Service
metadata:
  name: echo-app
  namespace: echo-app
  labels:
    app: echo-app
spec:
  selector:
    app: echo-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
